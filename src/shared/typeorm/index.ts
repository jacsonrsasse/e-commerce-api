// Esse arquivo inicia a conexão com o banco de dados, ele identifica o arquivo ormconfig.json e usa
// ele para se conectar com o banco.

import { createConnection } from 'typeorm';

createConnection();
